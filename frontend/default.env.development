# If overrides to .env values are needed for development:
# Copy to .env.development and run application using `npm run dev` to override base .env values

# local vite dev server port override
# Only used for non-docker development frontend deployments
DEV_SERVER_PORT=3000